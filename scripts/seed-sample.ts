/**
 * Seed script README
 *
 * The actual seed mutation lives in convex/seed.ts
 *
 * Usage (after running `npx convex dev`):
 *   npx convex run seed:seedSample
 *
 * This will create a sample firm + audit and return the slug URL.
 */
